import { createMachine } from 'xstate';

export const dataFetchingMachine = createMachine({
  id: 'fetchingData',
  tsTypes: {} as import('./data-fetching-machine.typegen').Typegen0,
  //
  // TODO
  //
});
